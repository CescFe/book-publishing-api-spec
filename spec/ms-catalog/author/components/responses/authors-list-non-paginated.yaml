description: List of all authors retrieved successfully
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '../schemas/author-summary.yaml'
        meta:
          $ref: '../../../shared/components/schemas/meta.yaml#/MetaNonPaginated'
      required:
        - data
        - meta

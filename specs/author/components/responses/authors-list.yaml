description: List of authors retrieved successfully
content:
  application/json:
    schema:
      allOf:
        - $ref: '../../../shared/components/schemas/paginated-response.yaml#/PaginatedResponse'
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: '../schemas/author.yaml'

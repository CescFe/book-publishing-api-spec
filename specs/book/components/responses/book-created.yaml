description: Book created successfully
headers:
  Location:
    description: URL of the created book
    schema:
      type: string
      format: uri
      example: "/api/v1/books/123e4567-e89b-12d3-a456-426614174000"
content:
  application/json:
    schema:
      $ref: '../schemas/book.yaml'
